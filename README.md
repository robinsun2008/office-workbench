# 本地办公工作台

一个轻量级的本地办公Web应用，支持HTTPS加密传输。

## 功能模块

- **待办任务管理** - 多条件检索、附件上传、进度跟踪、首页到期提醒
- **工作备忘录** - 万字级长文本存储、自定义标签、附件支持
- **重点关注看板** - 可视化展示关注领域及事项状态
- **部门报告库** - 存储各类部门文档、全文检索、分类管理

## 技术栈

- 前端：原生 HTML + JavaScript
- 后端：Python Flask
- 数据库：SQLite（本地存储）
- 安全：HTTPS加密传输

## 快速启动

```bash
# 安装依赖
pip install -r requirements.txt

# 初始化数据库
python init_db.py

# 生成SSL证书（可选，用于HTTPS）
python generate_ssl.py

# 启动应用
python app.py

# 访问地址
# HTTP: http://localhost:5000
# HTTPS: https://localhost:5000
```

## 数据导入导出

- 支持全量数据导出（ZIP格式）
- 支持一键导入恢复
- 附件批量导出，带标识可溯源

## 项目结构

```
├── app.py              # Flask主应用
├── config.py           # 配置文件
├── init_db.py          # 数据库初始化
├── generate_ssl.py     # SSL证书生成
├── requirements.txt    # Python依赖
├── static/             # 静态资源
├── templates/          # HTML模板
├── data/               # 数据库目录
├── uploads/            # 附件存储
└── ssl/                # SSL证书
```

## 许可证

MIT License

## 更新日志

### 2026-07-03 - 功能增强v1.1

实现以下5项需求优化：

| 需求编号 | 功能名称 | 实现要点 |
|---------|---------|---------|
| REQ-001 | 密码保护机制 | 创建登录页面，默认密码admin123；实现登录/登出/修改密码API；所有业务路由添加认证保护；首页添加修改密码和退出登录按钮 |
| REQ-002 | 待办任务信息密度优化 | 任务卡片改为紧凑布局，标签/状态/摘要水平排列；详细内容（描述、团队、进度等）默认折叠，点击展开；截止日期单独高亮显示；移除"展开详情"独占行，创建时间移入折叠区域；卡片间距从20px压缩到6px，内边距从12px压缩到6px；单屏任务数量提升约100% |
| REQ-003 | 部门报告库附件上传 | 新增报告附件API（上传/获取）；前端报告卡片添加上传附件按钮；支持附件列表展示、下载和删除 |
| REQ-004 | 首页即将到期任务排序优化 | 显示范围从3天扩展到5天；按标签优先级排序（紧急>重要>一般）；同级按到期时间升序 |
| REQ-005 | 文本汇总导出功能 | 新增文本汇总导出API；导出内容包含待办任务（含详细字段）、备忘录（含正文）、重点关注（含领域名称）、部门报告清单；首页添加导出按钮 |

### 安全特性

- **HTTPS加密传输**：支持自签名SSL证书，数据传输加密
- **密码保护**：单一密码认证，SHA256哈希存储，支持修改密码
- **会话管理**：基于Flask Session的登录状态管理

### 2026-07-08 - 功能增强v1.3

新增三项优化需求：

| 需求编号 | 功能名称 | 实现要点 |
|---------|---------|---------|
| REQ-007 | 首页任务详情与编辑 | 点击首页即将到期任务弹出详情模态框；显示完整任务信息；提供编辑按钮跳转至待办页面编辑 |
| REQ-008 | 附件文件名编辑 | 新增PUT /api/attachments/<id>/rename API；支持点击文件名或重命名按钮编辑；Enter确认/Escape取消；文件名长度限制200字符；禁止特殊字符 |
| REQ-009 | 扩展附件文件类型 | 新增支持.msg/.eml邮件格式；.7z/.tar/.gz压缩格式；.md/.markdown Markdown格式；.csv/.json/.xml/.pptx文档格式；禁止上传.exe/.bat等可执行文件 |

### 2026-07-10 - 功能增强v1.4

新增七项优化需求：

| 需求编号 | 功能名称 | 实现要点 |
|---------|---------|---------|
| REQ-010 | 登录密码每日一次 | 会话持久化24小时；登录成功后`session.permanent = True`；配置`PERMANENT_SESSION_LIFETIME = timedelta(days=1)` |
| REQ-011 | 版本控制显示版本号 | 配置文件定义VERSION常量；所有页面底部显示版本号；版本号随功能更新递增 |
| REQ-012 | 待办任务和重点关注软删除 | 数据库迁移添加is_deleted/deleted_at字段；删除操作标记记录而非物理删除；查询时过滤已删除记录 |
| REQ-013 | 导出文本汇总包含删除内容 | 正常记录后追加已删除记录；标注"【已删除】"标识；显示删除时间 |
| REQ-014 | 删除清理功能 | 新增DELETE /api/cleanup/deleted API；永久删除所有已标记记录及附件；系统配置页面提供清理按钮 |
| REQ-015 | 备忘录和报告库直接删除 | 保持原有直接删除逻辑；删除记录同时删除关联附件文件 |
| REQ-016 | 重点关注事项附件上传 | 新增POST/GET /api/focus/items/<id>/attachments API；支持上传、下载、重命名、删除附件；与其他模块保持一致 |